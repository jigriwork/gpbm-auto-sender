# Local Development

Install dependencies:

```bash
npm install
```

Run the web dashboard:

```bash
npm run dev
```

Build and check:

```bash
npm run typecheck
npm run build
```

The agent requires environment variables from `apps/agent/.env.example`. It is currently a structural placeholder and does not require a live Supabase project.
